<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hyundaimotors.hmb.cdppapp.mapper.IFHMINNOCEANTEST0000Mapper">

    <insert id="insertObject" parameterType="IFHMINNOCEANTEST0000UserDto">
        insert into landing.if_test
        (
            r_id
            , r_content
        ) values (
            #{rId}
            , #{rContent}
        )
    </insert>

    
</mapper>